/* empty css                                                                    */
import { d as createComponent, i as renderComponent, r as renderTemplate, u as unescapeHTML } from './astro/server_C7nAViGe.mjs';
import 'kleur/colors';
import { $ as $$BlogLayout } from './BlogLayout_COI89YL8.mjs';

const html = () => "<h1 id=\"creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-en-azure\">Creando una biblioteca simple usando CLI angular 7 y desplegando en azure</h1>\n<p>Angular CLI 7 ya está aquí! Con ello, el equipo de Angular ha facilitado mucho la creación de bibliotecas.</p>\n<hr>\n<h3 id=\"creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-enazure\">Creando una biblioteca simple usando CLI angular 7 y desplegando en azure</h3>\n<p>Angular CLI 7 ya está aquí! Con ello, el equipo de Angular ha facilitado mucho la creación de bibliotecas.</p>\n<p>En este artículo, mostramos paso a paso cómo crear una biblioteca simple utilizando Angular CLI 6. Vamos a sumergirnos en …</p>\n<h3 id=\"preparar\">Preparar</h3>\n<p>Vamos a configurar para crear nuestra biblioteca. Si ya tiene instalado Angular CLI 7, pase a la siguiente sección.</p>\n<h4 id=\"instala-nodejs\">Instala Node.js</h4>\n<p>Para empezar, necesitamos instalar Node.js. Puede descargar el último instalador de LTS de Node.js en <a href=\"https://nodejs.org/dist/v8.11.2/node-v8.11.2.pkg\">https://nodejs.org/dist/v8.11.2/node-v8.11.2.pkg</a> . Una vez que el instalador haya finalizado, ejecute el siguiente comando en el terminal para volver a verificar todo lo que funcionó:</p>\n<p><code>$ node --version</code></p>\n<h4 id=\"instalar-cliangular\">Instalar CLI angular</h4>\n<p>Después de instalar Node.js, podemos instalar Angular CLI. En una ventana de terminal, emita el siguiente comando:</p>\n<p><code>$ npm install -g @angular/cli</code></p>\n<p>Una vez finalizada la instalación, podemos emitir el siguiente comando para asegurarnos de que tenemos todo en su lugar:</p>\n<p><code>$ ng --version</code></p>\n<h3 id=\"proyecto\">Proyecto</h3>\n<p>Ahora que hemos finalizado la configuración, podemos seguir adelante y crear nuestro nuevo proyecto Angular emitiendo el <code>ng new</code>comando:</p>\n<p><code>$ ng new angular-library --prefix al</code></p>\n<p>Ahora pasemos a nuestro nuevo proyecto con:</p>\n<p><code>$ cd angular-library</code></p>\n<p>Si es nuevo en la versión 6 de Angular CLI, notará que el <code>.angular-cli.json</code>archivo ha sido reemplazado por <code>angular.json</code>. Como novedad en esta versión, este archivo define su área de trabajo para el proyecto que se acaba de crear. Dentro de ti encontrarás dos proyectos que se crean con el nuevo comando. Tenemos nuestro <code>angular-library</code>proyecto y nuestro <code>angular-library-e2e</code>proyecto.</p>\n<p>Para obtener más información sobre el <code>angular.json</code>archivo, visite la página de documentación de <a href=\"https://github.com/angular/angular-cli/wiki/angular-workspace\">Angular Workspace</a> .</p>\n<p>Ahora no solo podemos generar múltiples aplicaciones fácilmente en un nuevo proyecto Angular, sino que también podemos generar nuestra biblioteca.</p>\n<h4 id=\"generar-la-biblioteca\">Generar la biblioteca</h4>\n<p>Para esta demostración, <a href=\"https://reqres.in/\">usaré REQ | RES</a> , una API de REST alojada que nos permitirá poner todo en funcionamiento rápidamente. Para generar nuestra biblioteca para REQ | En el proyecto RES, utilizaremos el <code>ng generate</code>comando.</p>\n<p><code>$ ng generate library reqres --prefix rr</code></p>\n<p>Este comando ha hecho dos cosas dentro de nuestro proyecto. En el <code>angular.json</code>archivo ahora tenemos <code>reqres</code>proyecto debajo del <code>angular-libraries-e2e</code>. La segunda es que en nuestro proyecto ahora tenemos una <code>projects</code>carpeta que contiene todos los archivos que necesita nuestra biblioteca para comenzar.</p>\n<h4 id=\"crear-losmodelos\">Crear los modelos</h4>\n<p>Lo siguiente que queremos hacer es crear un par de modelos para nuestra biblioteca. Para esta demostración solo nos centraremos en el REQ | RES obtener la lista de usuarios de la API. Si vamos a <a href=\"https://reqres.in/api/users?page=1\">https://reqres.in/api/users?page=1</a> , vemos que el JSON devuelto será:</p>\n<p><code>json</code></p>\n<p><code>{</code></p>\n<p><code>\"page\": 1,</code></p>\n<p><code>\"per_page\": 3,</code></p>\n<p><code>\"total\": 12,</code></p>\n<p><code>\"total_pages\": 4,</code></p>\n<p><code>\"data\": [</code></p>\n<p><code>{</code></p>\n<p><code>\"id\": 1,</code></p>\n<p><code>\"first_name\": \"George\",</code></p>\n<p><code>\"last_name\": \"Bluth\",</code></p>\n<p><code>\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/calebogden/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/calebogden/128.jpg)\"</code></p>\n<p><code>},</code></p>\n<p><code>{</code></p>\n<p><code>\"id\": 2,</code></p>\n<p><code>\"first_name\": \"Janet\",</code></p>\n<p><code>\"last_name\": \"Weaver\",</code></p>\n<p><code>\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/josephstein/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/josephstein/128.jpg)\"</code></p>\n<p><code>},</code></p>\n<p><code>{</code></p>\n<p><code>\"id\": 3,</code></p>\n<p><code>\"first_name\": \"Emma\",</code></p>\n<p><code>\"last_name\": \"Wong\",</code></p>\n<p><code>\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/olegpogodaev/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/olegpogodaev/128.jpg)\"</code></p>\n<p><code>}</code></p>\n<p><code>]</code></p>\n<p><code>}</code></p>\n<p>Vamos a crear la <code>projects/reqres/src/lib/model</code>carpeta y nuestras dos nuevas clases <code>users</code>y <code>user</code>. Ingrese lo siguiente en el termianl:</p>\n<p><code>$ ng generate class model/user --project=reqres</code></p>\n<p><code>$ ng generate class model/users --project=reqres</code></p>\n<p><code>export class User {</code></p>\n<p><code>constructor(</code></p>\n<p><code>public id: number,</code></p>\n<p><code>public first_name: string,</code></p>\n<p><code>public last_name: string,</code></p>\n<p><code>public avatar: string</code></p>\n<p><code>) {}</code></p>\n<p><code>}</code></p>\n<p>Nuestro <code>users</code>modelo será:</p>\n<p><code>import { User } from './user';</code></p>\n<p><code>export class Users {</code></p>\n<p><code>constructor(</code></p>\n<p><code>public page: number,</code></p>\n<p><code>public per_page: number,</code></p>\n<p><code>public total: number,</code></p>\n<p><code>public total_pages: number,</code></p>\n<p><code>public data: User[]</code></p>\n<p><code>) {}</code></p>\n<p><code>}</code></p>\n<p>Ahora hagamos nuestros nuevos modelos disponibles para su uso. En el <code>projects/reqres/src/public_api.ts</code>archivo, agregue lo siguiente:</p>\n<p><code>export * from './lib/model/user';</code></p>\n<p><code>export * from './lib/model/users';</code></p>\n<p>Nuestro <code>public_api</code>archivo completo debería verse como sigue:</p>\n<p><code>export * from './lib/reqres.service';</code></p>\n<p><code>export * from './lib/reqres.component';</code></p>\n<p><code>export * from './lib/reqres.module';</code></p>\n<p><code>export * from './lib/model/user';</code></p>\n<p><code>export * from './lib/model/users';</code></p>\n<h4 id=\"crear-elservicio\">Crear el servicio</h4>\n<p>Angular CLI ya ha generado el <code>projects/reqres/src/lib/reqres.service.ts</code>archivo para nosotros, por lo que todo lo que tenemos que hacer es agregar nuestro código para llamar a la API. Vamos a abrir <code>reqres.service.ts</code>y editarlo para que el resultado final sea el siguiente:</p>\n<p><code>import { Injectable } from '@angular/core';</code></p>\n<p><code>import { HttpClient, HttpParams } from '@angular/common/http';</code></p>\n<p><code>import { Observable } from 'rxjs';</code></p>\n<p><code>import { Users } from './model/users';</code></p>\n<p><code>@Injectable({</code></p>\n<p><code>providedIn: 'root'</code></p>\n<p><code>})</code></p>\n<p><code>export class ReqresService {</code></p>\n<p><code>private readonly apiRoot = '[https://reqres.in/api](https://reqres.in/api)';</code></p>\n<p><code>constructor(private httpClient: HttpClient) { }</code></p>\n<p><code>getUsers(page: number): Observable&#x3C;Users> {</code></p>\n<p><code>const params = new HttpParams().set('page', String(page));</code></p>\n<p><code>return this.httpClient.get&#x3C;Users>(`${this.apiRoot}/users`, {params: params});</code></p>\n<p><code>}</code></p>\n<p><code>}</code></p>\n<p>Este servicio es bastante sencillo, solo un simple ‘get’ para la API. Una cosa a tener en cuenta es la propiedad <code>providedIn: 'root'</code>. Esto es nuevo para Angular 6 y especifica en qué módulo se configurará el servicio, en este caso <code>root</code>,. Esto permite que la sacudida de árboles ocurra para los servicios ahora.</p>\n<h4 id=\"crear-los-componentes\">Crear los componentes</h4>\n<p>Ahora vamos a generar el componente de usuarios para nuestra biblioteca. Para hacer esto, volvamos a nuestro terminal y usemos la CLI para generar nuestros archivos.</p>\n<p><code>$ ng generate component users --project=reqres</code></p>\n<p>Todo lo que haremos en este componente es generar una lista de los usuarios. El código final del componente será el siguiente:</p>\n<p><code>import { Component, OnInit, Input } from '@angular/core';</code></p>\n<p><code>import { ReqresService } from '../../public_api';</code></p>\n<p><code>import { Users } from '../model/users';</code></p>\n<p><code>@Component({</code></p>\n<p><code>selector: 'rr-user',</code></p>\n<p><code>templateUrl: './users.component.html',</code></p>\n<p><code>styleUrls: ['./users.component.css']</code></p>\n<p><code>})</code></p>\n<p><code>export class UsersComponent implements OnInit {</code></p>\n<p><code>users: Users;</code></p>\n<p><code>@Input() pageNumber: number;</code></p>\n<p><code>constructor(private reqresService: ReqresService) { }</code></p>\n<p><code>ngOnInit() {</code></p>\n<p><code>this.getUsers();</code></p>\n<p><code>}</code></p>\n<p><code>getUsers(): void {</code></p>\n<p><code>this.reqresService.getUsers(this.pageNumber).subscribe(arg => this.users = arg);</code></p>\n<p><code>}</code></p>\n<p><code>}</code></p>\n<p>El HTML para el componente es solo la siguiente lista simple:</p>\n<p><code>&#x3C;div></code></p>\n<p><code>&#x3C;ul></code></p>\n<p><code>&#x3C;li *ngFor=\"let user of users.data\"></code></p>\n<p><code>{{user.last_name}}, {{user.first_name}}</code></p>\n<p><code>&#x3C;/li></code></p>\n<p><code>&#x3C;/ul></code></p>\n<p><code>&#x3C;/div></code></p>\n<p>Ahora que hemos creado el componente, asegurémonos de que nuestro módulo tenga todo lo que necesita.</p>\n<p>En el <code>reqres.module.ts</code>archivo necesitamos importar el <code>CommonModule</code>y el <code>HttpClientModule</code>. El CLI agregó nuestras declaraciones para el <code>ReqresComponent</code>y el <code>UsersComponent</code>.</p>\n<p>Ahora debemos agregar el <code>UsersComponent</code>a el <code>exports</code>para que se pueda usar fuera del módulo y nuestra biblioteca esté lista. El módulo final debería verse así:</p>\n<p><code>import { NgModule } from '@angular/core';</code></p>\n<p><code>import { ReqresComponent } from './reqres.component';</code></p>\n<p><code>import { CommonModule } from '@angular/common';</code></p>\n<p><code>import { HttpClientModule } from '@angular/common/http';</code></p>\n<p><code>import { UsersComponent } from './users/users.component';</code></p>\n<p><code>@NgModule({</code></p>\n<p><code>imports: [CommonModule, HttpClientModule],</code></p>\n<p><code>declarations: [ReqresComponent, UsersComponent],</code></p>\n<p><code>exports: [UsersComponent]</code></p>\n<p><code>})</code></p>\n<p><code>export class ReqresModule { }</code></p>\n<h4 id=\"construir-la-biblioteca\">Construir la biblioteca</h4>\n<p>Ahora que tenemos todo listo para usar, necesitamos construir nuestra biblioteca. Esto es tan fácil como el siguiente comando:</p>\n<p><code>$ ng build reqres</code></p>\n<p>Con la compilación completa, podemos seguir adelante y usarla en nuestra aplicación raíz.</p>\n<p><a href=\"https://keyholesoftware.com/2018/05/21/getting-started-with-xamarin-forms-and-azure-mobile-app-service/\"><strong>Vea también:</strong> Introducción a Xamarin.Forms y el Servicio de aplicación móvil de Azure</a></p>\n<h4 id=\"usar-la-biblioteca\">Usar la biblioteca</h4>\n<p>Para usar la biblioteca primero necesitamos importarla. En la <code>src/app/app.module.ts</code>suma <code>ReqresModule</code>de las importaciones. La importación del módulo debe verse como:</p>\n<p><code>import { ReqresModule } from 'reqres';</code></p>\n<p>La final <code>app.module.ts</code>será:</p>\n<p><code>import { BrowserModule } from '@angular/platform-browser';</code></p>\n<p><code>import { NgModule } from '@angular/core';</code></p>\n<p><code>import { AppComponent } from './app.component';</code></p>\n<p><code>import { ReqresModule } from 'reqres';</code></p>\n<p><code>@NgModule({</code></p>\n<p><code>declarations: [</code></p>\n<p><code>AppComponent</code></p>\n<p><code>],</code></p>\n<p><code>imports: [</code></p>\n<p><code>BrowserModule,</code></p>\n<p><code>ReqresModule</code></p>\n<p><code>],</code></p>\n<p><code>providers: [],</code></p>\n<p><code>bootstrap: [AppComponent]</code></p>\n<p><code>})</code></p>\n<p><code>export class AppModule { }</code></p>\n<p>Observe que no estamos importando el módulo utilizando una ruta relativa. Podemos configurar esto como la biblioteca estaba en nuestros node_modules porque cuando la CLI creó la biblioteca. También agregó lo siguiente a la <code>tsconfig.json</code>:</p>\n<p><code>\"paths\": {</code></p>\n<p><code>\"reqres\": [</code></p>\n<p><code>\"dist/reqres\"</code></p>\n<p><code>]</code></p>\n<p><code>}</code></p>\n<p>Una vez que el módulo se publica y se introduce en nuestra aplicación a través de node_modules, todo lo que tendremos que hacer es volver a <code>tsconfig.json</code>la ruta y eliminarla.</p>\n<p>El último paso será utilizar nuestra lista en nuestro componente. Añadir “a <code>app.component.html</code>.</p>\n<p>Ahora que tenemos todo en su lugar, servimos la aplicación ejecutando:</p>\n<p><code>$ ng serve</code></p>\n<p>Una vez que el servidor esté en funcionamiento, vaya a <a href=\"http://localhost:4200/\">http: // localhost: 4200</a> mostrará una lista de tres usuarios en nuestra aplicación que se generó usando nuestra biblioteca.</p>\n<h4 id=\"publicar-la-biblioteca\">Publicar la biblioteca</h4>\n<p>Lo único que queda por hacer es publicar nuestra biblioteca. Si estamos publicando para <code>NPM</code>esto está emitiendo algunos comandos desde el terminal. Solo recuerda que para publicar en NPM necesitas una cuenta.</p>\n<p><code>$ ng build reqres --prod</code></p>\n<p><code>$ cd dist/reqres</code></p>\n<p><code>$ npm publish</code></p>\n<p>En nuestro caso desplegaremos con integracion continua desde GitHub</p>\n<p><strong>Desplegando</strong></p>\n<ol>\n<li>Ve al <code>Build and Release</code>menú y selecciona<code>Builds</code></li>\n<li>Haga clic en el <code>New</code>botón para crear una nueva construcción</li>\n<li>Comenzar con un <code>empty process</code></li>\n</ol>\n<p><strong>Obtener su codigo</strong></p>\n<ol>\n<li>Debajo <code>Process</code>, haga clic en<code>Get Sources</code></li>\n<li>Seleccione Github, ingrese su autorización y seleccione su repositorio y sucursal</li>\n</ol>\n<p><strong>Instale el CLI angular</strong></p>\n<ol>\n<li>Haga clic <code>Add Task</code>, a la izquierda, busque <code>npm</code>a la derecha</li>\n<li>Seleccione <code>npm (run an npm command)</code>y haga clic<code>Add</code></li>\n<li>Ingrese un nombre como <code>install the angular cli</code></li>\n<li>Ingrese el comando npm como <code>install</code></li>\n<li>Ingrese los argumentos como <code>@angular/cli -g</code></li>\n</ol>\n<p><strong>Instalar los paquetes NPM del proyecto</strong></p>\n<ol>\n<li>Haga clic <code>Add Task</code>, a la izquierda, busque <code>npm</code>a la derecha</li>\n<li>Seleccione <code>npm (run an npm command)</code>y haga clic<code>Add</code></li>\n<li>Ingrese un nombre como <code>install packages via npm</code></li>\n<li>Ingrese el comando npm como <code>install</code></li>\n</ol>\n<p><strong>Construye el Proyecto con el CLI Angular</strong></p>\n<ol>\n<li>Haga clic <code>Add Task</code>, a la izquierda, busque <code>command line</code>a la derecha</li>\n<li>Seleccione <code>Command Line</code>y haga clic<code>Add</code></li>\n<li>Ingrese un nombre como <code>build the angular app</code></li>\n<li>Introduzca la herramienta como <code>ng</code></li>\n<li>Ingrese los argumentos como <code>build --prod</code></li>\n</ol>\n<p><strong>Instale los paquetes NPM del servidor Express</strong></p>\n<ol>\n<li>Haga clic <code>Add Task</code>, a la izquierda, busque <code>npm</code>a la derecha</li>\n<li>Seleccione <code>npm (run an npm command)</code>y haga clic<code>Add</code></li>\n<li>Ingrese un nombre como <code>npm install in the dist folder for express</code></li>\n<li>Entra en la carpeta de trabajo como <code>dist/</code></li>\n<li>Ingrese el comando npm como <code>install</code></li>\n</ol>\n<p><strong>Implementar a Azure</strong></p>\n<ol>\n<li>Haga clic <code>Add Task</code>, a la izquierda, busque <code>azure app service deploy</code>a la derecha</li>\n<li>Seleccione <code>Azure App Service Deploy</code>y haga clic<code>Add</code></li>\n<li>Ingrese un nombre como <code>Azure App Service Deploy: my-app</code></li>\n<li>Seleccione su suscripción de Azure</li>\n<li>Seleccione el nombre del servicio de su aplicación</li>\n<li>Establecer la <code>package or folder</code>a<code>dist/</code></li>\n<li>Seleccione la casilla de verificación para <code>Generate Web.config</code></li>\n<li>Introduzca el <code>Web.config parameters</code>como<code>-Handler iisnode -NodeStartFile index.js -appType node</code></li>\n<li>Seleccione la casilla de verificación para <code>Publish using Web Deploy</code></li>\n<li>Marque la casilla para <code>Remove additional files at destination</code></li>\n</ol>\n<p><strong>Ejecute este proceso en todos los compromisos / fusiones con Github</strong></p>\n<ol>\n<li>Ir a <code>triggers</code>en la parte superior izquierda y <code>enable</code>la integración continua.</li>\n<li>Presiona el <code>Save and Queue</code>boton</li>\n</ol>\n<p>¡Entonces puedes ponerlo en cola y ver el progreso de la construcción!</p>\n<h3 id=\"pensamientos-finales\">Pensamientos finales</h3>\n<p>¡Y eso es todo lo que necesitas para sacar tus propias bibliotecas para el mundo!</p>\n<p>Espero que este tutorial le haya ayudado a tener una buena idea de lo fácil que es con Angular CLI 7configurar sus propias bibliotecas y publicarlas para su uso. Espero ver qué nuevos y emocionantes módulos se crean. ¡Por favor hazme saber si tienes preguntas!</p>\n<p>By <a href=\"https://medium.com/@devjaime\">Jaime Hernández</a> on <a href=\"https://medium.com/p/d8c9d66668bd\">November 7, 2018</a>.</p>\n<p><a href=\"https://medium.com/@devjaime/creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-en-azure-d8c9d66668bd\">Canonical link</a></p>\n<p>Exported from <a href=\"https://medium.com\">Medium</a> on March 15, 2025.</p>";

				const frontmatter = {"layout":"../../layouts/BlogLayout.astro","title":"Creando una biblioteca simple usando CLI angular 7 y desplegando en azure","description":"","tags":["code","Angular"],"time":4,"featured":true,"timestamp":"2018-11-07T12:20:31-0300","filename":"2018-11-07_Creando-una-biblioteca-simple-usando-CLI-angular-7-y-desplegando-en-azure-d8c9d66668bd"};
				const file = "/Users/devjaime/Documents/devjaimeblog/src/pages/blog/2018-11-07_Creando-una-biblioteca-simple-usando-CLI-angular-7-y-desplegando-en-azure-d8c9d66668bd.md";
				const url = "/blog/2018-11-07_Creando-una-biblioteca-simple-usando-CLI-angular-7-y-desplegando-en-azure-d8c9d66668bd";
				function rawContent() {
					return "   \n                                        \n                                                                                  \n               \n                         \n       \n              \n                                     \n                                                                                                             \n   \n\nCreando una biblioteca simple usando CLI angular 7 y desplegando en azure\n=========================================================================\n\nAngular CLI 7 ya está aquí! Con ello, el equipo de Angular ha facilitado mucho la creación de bibliotecas.\n\n* * *\n\n### Creando una biblioteca simple usando CLI angular 7 y desplegando en azure\n\nAngular CLI 7 ya está aquí! Con ello, el equipo de Angular ha facilitado mucho la creación de bibliotecas.\n\nEn este artículo, mostramos paso a paso cómo crear una biblioteca simple utilizando Angular CLI 6. Vamos a sumergirnos en …\n\n### Preparar\n\nVamos a configurar para crear nuestra biblioteca. Si ya tiene instalado Angular CLI 7, pase a la siguiente sección.\n\n#### Instala Node.js\n\nPara empezar, necesitamos instalar Node.js. Puede descargar el último instalador de LTS de Node.js en [https://nodejs.org/dist/v8.11.2/node-v8.11.2.pkg](https://nodejs.org/dist/v8.11.2/node-v8.11.2.pkg) . Una vez que el instalador haya finalizado, ejecute el siguiente comando en el terminal para volver a verificar todo lo que funcionó:\n\n`$ node --version`\n\n#### Instalar CLI angular\n\nDespués de instalar Node.js, podemos instalar Angular CLI. En una ventana de terminal, emita el siguiente comando:\n\n`$ npm install -g @angular/cli`\n\nUna vez finalizada la instalación, podemos emitir el siguiente comando para asegurarnos de que tenemos todo en su lugar:\n\n`$ ng --version`\n\n### Proyecto\n\nAhora que hemos finalizado la configuración, podemos seguir adelante y crear nuestro nuevo proyecto Angular emitiendo el `ng new`comando:\n\n`$ ng new angular-library --prefix al`\n\nAhora pasemos a nuestro nuevo proyecto con:\n\n`$ cd angular-library`\n\nSi es nuevo en la versión 6 de Angular CLI, notará que el `.angular-cli.json`archivo ha sido reemplazado por `angular.json`. Como novedad en esta versión, este archivo define su área de trabajo para el proyecto que se acaba de crear. Dentro de ti encontrarás dos proyectos que se crean con el nuevo comando. Tenemos nuestro `angular-library`proyecto y nuestro `angular-library-e2e`proyecto.\n\nPara obtener más información sobre el `angular.json`archivo, visite la página de documentación de [Angular Workspace](https://github.com/angular/angular-cli/wiki/angular-workspace) .\n\nAhora no solo podemos generar múltiples aplicaciones fácilmente en un nuevo proyecto Angular, sino que también podemos generar nuestra biblioteca.\n\n#### Generar la biblioteca\n\nPara esta demostración, [usaré REQ | RES](https://reqres.in/) , una API de REST alojada que nos permitirá poner todo en funcionamiento rápidamente. Para generar nuestra biblioteca para REQ | En el proyecto RES, utilizaremos el `ng generate`comando.\n\n`$ ng generate library reqres --prefix rr`\n\nEste comando ha hecho dos cosas dentro de nuestro proyecto. En el `angular.json`archivo ahora tenemos `reqres`proyecto debajo del `angular-libraries-e2e`. La segunda es que en nuestro proyecto ahora tenemos una `projects`carpeta que contiene todos los archivos que necesita nuestra biblioteca para comenzar.\n\n#### Crear los modelos\n\nLo siguiente que queremos hacer es crear un par de modelos para nuestra biblioteca. Para esta demostración solo nos centraremos en el REQ | RES obtener la lista de usuarios de la API. Si vamos a [https://reqres.in/api/users?page=1](https://reqres.in/api/users?page=1) , vemos que el JSON devuelto será:\n\n`json`\n\n`{`\n\n`\"page\": 1,`\n\n`\"per_page\": 3,`\n\n`\"total\": 12,`\n\n`\"total_pages\": 4,`\n\n`\"data\": [`\n\n`{`\n\n`\"id\": 1,`\n\n`\"first_name\": \"George\",`\n\n`\"last_name\": \"Bluth\",`\n\n`\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/calebogden/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/calebogden/128.jpg)\"`\n\n`},`\n\n`{`\n\n`\"id\": 2,`\n\n`\"first_name\": \"Janet\",`\n\n`\"last_name\": \"Weaver\",`\n\n`\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/josephstein/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/josephstein/128.jpg)\"`\n\n`},`\n\n`{`\n\n`\"id\": 3,`\n\n`\"first_name\": \"Emma\",`\n\n`\"last_name\": \"Wong\",`\n\n`\"avatar\": \"[https://s3.amazonaws.com/uifaces/faces/twitter/olegpogodaev/128.jpg](https://s3.amazonaws.com/uifaces/faces/twitter/olegpogodaev/128.jpg)\"`\n\n`}`\n\n`]`\n\n`}`\n\nVamos a crear la `projects/reqres/src/lib/model`carpeta y nuestras dos nuevas clases `users`y `user`. Ingrese lo siguiente en el termianl:\n\n`$ ng generate class model/user --project=reqres`\n\n`$ ng generate class model/users --project=reqres`\n\n`export class User {`\n\n`constructor(`\n\n`public id: number,`\n\n`public first_name: string,`\n\n`public last_name: string,`\n\n`public avatar: string`\n\n`) {}`\n\n`}`\n\nNuestro `users`modelo será:\n\n`import { User } from './user';`\n\n`export class Users {`\n\n`constructor(`\n\n`public page: number,`\n\n`public per_page: number,`\n\n`public total: number,`\n\n`public total_pages: number,`\n\n`public data: User[]`\n\n`) {}`\n\n`}`\n\nAhora hagamos nuestros nuevos modelos disponibles para su uso. En el `projects/reqres/src/public_api.ts`archivo, agregue lo siguiente:\n\n`export * from './lib/model/user';`\n\n`export * from './lib/model/users';`\n\nNuestro `public_api`archivo completo debería verse como sigue:\n\n`export * from './lib/reqres.service';`\n\n`export * from './lib/reqres.component';`\n\n`export * from './lib/reqres.module';`\n\n`export * from './lib/model/user';`\n\n`export * from './lib/model/users';`\n\n#### Crear el servicio\n\nAngular CLI ya ha generado el `projects/reqres/src/lib/reqres.service.ts`archivo para nosotros, por lo que todo lo que tenemos que hacer es agregar nuestro código para llamar a la API. Vamos a abrir `reqres.service.ts`y editarlo para que el resultado final sea el siguiente:\n\n`import { Injectable } from '@angular/core';`\n\n`import { HttpClient, HttpParams } from '@angular/common/http';`\n\n`import { Observable } from 'rxjs';`\n\n`import { Users } from './model/users';`\n\n`@Injectable({`\n\n`providedIn: 'root'`\n\n`})`\n\n`export class ReqresService {`\n\n`private readonly apiRoot = '[https://reqres.in/api](https://reqres.in/api)';`\n\n`constructor(private httpClient: HttpClient) { }`\n\n`getUsers(page: number): Observable<Users> {`\n\n`const params = new HttpParams().set('page', String(page));`\n\n``return this.httpClient.get<Users>(`${this.apiRoot}/users`, {params: params});``\n\n`}`\n\n`}`\n\nEste servicio es bastante sencillo, solo un simple ‘get’ para la API. Una cosa a tener en cuenta es la propiedad `providedIn: 'root'`. Esto es nuevo para Angular 6 y especifica en qué módulo se configurará el servicio, en este caso `root`,. Esto permite que la sacudida de árboles ocurra para los servicios ahora.\n\n#### Crear los componentes\n\nAhora vamos a generar el componente de usuarios para nuestra biblioteca. Para hacer esto, volvamos a nuestro terminal y usemos la CLI para generar nuestros archivos.\n\n`$ ng generate component users --project=reqres`\n\nTodo lo que haremos en este componente es generar una lista de los usuarios. El código final del componente será el siguiente:\n\n`import { Component, OnInit, Input } from '@angular/core';`\n\n`import { ReqresService } from '../../public_api';`\n\n`import { Users } from '../model/users';`\n\n`@Component({`\n\n`selector: 'rr-user',`\n\n`templateUrl: './users.component.html',`\n\n`styleUrls: ['./users.component.css']`\n\n`})`\n\n`export class UsersComponent implements OnInit {`\n\n`users: Users;`\n\n`@Input() pageNumber: number;`\n\n`constructor(private reqresService: ReqresService) { }`\n\n`ngOnInit() {`\n\n`this.getUsers();`\n\n`}`\n\n`getUsers(): void {`\n\n`this.reqresService.getUsers(this.pageNumber).subscribe(arg => this.users = arg);`\n\n`}`\n\n`}`\n\nEl HTML para el componente es solo la siguiente lista simple:\n\n`<div>`\n\n`<ul>`\n\n`<li *ngFor=\"let user of users.data\">`\n\n`{{user.last_name}}, {{user.first_name}}`\n\n`</li>`\n\n`</ul>`\n\n`</div>`\n\nAhora que hemos creado el componente, asegurémonos de que nuestro módulo tenga todo lo que necesita.\n\nEn el `reqres.module.ts`archivo necesitamos importar el `CommonModule`y el `HttpClientModule`. El CLI agregó nuestras declaraciones para el `ReqresComponent`y el `UsersComponent`.\n\nAhora debemos agregar el `UsersComponent`a el `exports`para que se pueda usar fuera del módulo y nuestra biblioteca esté lista. El módulo final debería verse así:\n\n`import { NgModule } from '@angular/core';`\n\n`import { ReqresComponent } from './reqres.component';`\n\n`import { CommonModule } from '@angular/common';`\n\n`import { HttpClientModule } from '@angular/common/http';`\n\n`import { UsersComponent } from './users/users.component';`\n\n`@NgModule({`\n\n`imports: [CommonModule, HttpClientModule],`\n\n`declarations: [ReqresComponent, UsersComponent],`\n\n`exports: [UsersComponent]`\n\n`})`\n\n`export class ReqresModule { }`\n\n#### Construir la biblioteca\n\nAhora que tenemos todo listo para usar, necesitamos construir nuestra biblioteca. Esto es tan fácil como el siguiente comando:\n\n`$ ng build reqres`\n\nCon la compilación completa, podemos seguir adelante y usarla en nuestra aplicación raíz.\n\n[**Vea también:** Introducción a Xamarin.Forms y el Servicio de aplicación móvil de Azure](https://keyholesoftware.com/2018/05/21/getting-started-with-xamarin-forms-and-azure-mobile-app-service/)\n\n#### Usar la biblioteca\n\nPara usar la biblioteca primero necesitamos importarla. En la `src/app/app.module.ts`suma `ReqresModule`de las importaciones. La importación del módulo debe verse como:\n\n`import { ReqresModule } from 'reqres';`\n\nLa final `app.module.ts`será:\n\n`import { BrowserModule } from '@angular/platform-browser';`\n\n`import { NgModule } from '@angular/core';`\n\n`import { AppComponent } from './app.component';`\n\n`import { ReqresModule } from 'reqres';`\n\n`@NgModule({`\n\n`declarations: [`\n\n`AppComponent`\n\n`],`\n\n`imports: [`\n\n`BrowserModule,`\n\n`ReqresModule`\n\n`],`\n\n`providers: [],`\n\n`bootstrap: [AppComponent]`\n\n`})`\n\n`export class AppModule { }`\n\nObserve que no estamos importando el módulo utilizando una ruta relativa. Podemos configurar esto como la biblioteca estaba en nuestros node\\_modules porque cuando la CLI creó la biblioteca. También agregó lo siguiente a la `tsconfig.json`:\n\n`\"paths\": {`\n\n`\"reqres\": [`\n\n`\"dist/reqres\"`\n\n`]`\n\n`}`\n\nUna vez que el módulo se publica y se introduce en nuestra aplicación a través de node\\_modules, todo lo que tendremos que hacer es volver a `tsconfig.json`la ruta y eliminarla.\n\nEl último paso será utilizar nuestra lista en nuestro componente. Añadir “a `app.component.html`.\n\nAhora que tenemos todo en su lugar, servimos la aplicación ejecutando:\n\n`$ ng serve`\n\nUna vez que el servidor esté en funcionamiento, vaya a [http: // localhost: 4200](http://localhost:4200/) mostrará una lista de tres usuarios en nuestra aplicación que se generó usando nuestra biblioteca.\n\n#### Publicar la biblioteca\n\nLo único que queda por hacer es publicar nuestra biblioteca. Si estamos publicando para `NPM`esto está emitiendo algunos comandos desde el terminal. Solo recuerda que para publicar en NPM necesitas una cuenta.\n\n`$ ng build reqres --prod`\n\n`$ cd dist/reqres`\n\n`$ npm publish`\n\nEn nuestro caso desplegaremos con integracion continua desde GitHub\n\n**Desplegando**\n\n1.  Ve al `Build and Release`menú y selecciona`Builds`\n2.  Haga clic en el `New`botón para crear una nueva construcción\n3.  Comenzar con un `empty process`\n\n**Obtener su codigo**\n\n1.  Debajo `Process`, haga clic en`Get Sources`\n2.  Seleccione Github, ingrese su autorización y seleccione su repositorio y sucursal\n\n**Instale el CLI angular**\n\n1.  Haga clic `Add Task`, a la izquierda, busque `npm`a la derecha\n2.  Seleccione `npm (run an npm command)`y haga clic`Add`\n3.  Ingrese un nombre como `install the angular cli`\n4.  Ingrese el comando npm como `install`\n5.  Ingrese los argumentos como `@angular/cli -g`\n\n**Instalar los paquetes NPM del proyecto**\n\n1.  Haga clic `Add Task`, a la izquierda, busque `npm`a la derecha\n2.  Seleccione `npm (run an npm command)`y haga clic`Add`\n3.  Ingrese un nombre como `install packages via npm`\n4.  Ingrese el comando npm como `install`\n\n**Construye el Proyecto con el CLI Angular**\n\n1.  Haga clic `Add Task`, a la izquierda, busque `command line`a la derecha\n2.  Seleccione `Command Line`y haga clic`Add`\n3.  Ingrese un nombre como `build the angular app`\n4.  Introduzca la herramienta como `ng`\n5.  Ingrese los argumentos como `build --prod`\n\n**Instale los paquetes NPM del servidor Express**\n\n1.  Haga clic `Add Task`, a la izquierda, busque `npm`a la derecha\n2.  Seleccione `npm (run an npm command)`y haga clic`Add`\n3.  Ingrese un nombre como `npm install in the dist folder for express`\n4.  Entra en la carpeta de trabajo como `dist/`\n5.  Ingrese el comando npm como `install`\n\n**Implementar a Azure**\n\n1.  Haga clic `Add Task`, a la izquierda, busque `azure app service deploy`a la derecha\n2.  Seleccione `Azure App Service Deploy`y haga clic`Add`\n3.  Ingrese un nombre como `Azure App Service Deploy: my-app`\n4.  Seleccione su suscripción de Azure\n5.  Seleccione el nombre del servicio de su aplicación\n6.  Establecer la `package or folder`a`dist/`\n7.  Seleccione la casilla de verificación para `Generate Web.config`\n8.  Introduzca el `Web.config parameters`como`-Handler iisnode -NodeStartFile index.js -appType node`\n9.  Seleccione la casilla de verificación para `Publish using Web Deploy`\n10.  Marque la casilla para `Remove additional files at destination`\n\n**Ejecute este proceso en todos los compromisos / fusiones con Github**\n\n1.  Ir a `triggers`en la parte superior izquierda y `enable`la integración continua.\n2.  Presiona el `Save and Queue`boton\n\n¡Entonces puedes ponerlo en cola y ver el progreso de la construcción!\n\n### Pensamientos finales\n\n¡Y eso es todo lo que necesitas para sacar tus propias bibliotecas para el mundo!\n\nEspero que este tutorial le haya ayudado a tener una buena idea de lo fácil que es con Angular CLI 7configurar sus propias bibliotecas y publicarlas para su uso. Espero ver qué nuevos y emocionantes módulos se crean. ¡Por favor hazme saber si tienes preguntas!\n\nBy [Jaime Hernández](https://medium.com/@devjaime) on [November 7, 2018](https://medium.com/p/d8c9d66668bd).\n\n[Canonical link](https://medium.com/@devjaime/creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-en-azure-d8c9d66668bd)\n\nExported from [Medium](https://medium.com) on March 15, 2025.";
				}
				async function compiledContent() {
					return await html();
				}
				function getHeadings() {
					return [{"depth":1,"slug":"creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-en-azure","text":"Creando una biblioteca simple usando CLI angular 7 y desplegando en azure"},{"depth":3,"slug":"creando-una-biblioteca-simple-usando-cli-angular-7-y-desplegando-enazure","text":"Creando una biblioteca simple usando CLI angular 7 y desplegando en azure"},{"depth":3,"slug":"preparar","text":"Preparar"},{"depth":4,"slug":"instala-nodejs","text":"Instala Node.js"},{"depth":4,"slug":"instalar-cliangular","text":"Instalar CLI angular"},{"depth":3,"slug":"proyecto","text":"Proyecto"},{"depth":4,"slug":"generar-la-biblioteca","text":"Generar la biblioteca"},{"depth":4,"slug":"crear-losmodelos","text":"Crear los modelos"},{"depth":4,"slug":"crear-elservicio","text":"Crear el servicio"},{"depth":4,"slug":"crear-los-componentes","text":"Crear los componentes"},{"depth":4,"slug":"construir-la-biblioteca","text":"Construir la biblioteca"},{"depth":4,"slug":"usar-la-biblioteca","text":"Usar la biblioteca"},{"depth":4,"slug":"publicar-la-biblioteca","text":"Publicar la biblioteca"},{"depth":3,"slug":"pensamientos-finales","text":"Pensamientos finales"}];
				}

				const Content = createComponent((result, _props, slots) => {
					const { layout, ...content } = frontmatter;
					content.file = file;
					content.url = url;

					return renderTemplate`${renderComponent(result, 'Layout', $$BlogLayout, {
								file,
								url,
								content,
								frontmatter: content,
								headings: getHeadings(),
								rawContent,
								compiledContent,
								'server:root': true,
							}, {
								'default': () => renderTemplate`${unescapeHTML(html())}`
							})}`;
				});

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
	__proto__: null,
	Content,
	compiledContent,
	default: Content,
	file,
	frontmatter,
	getHeadings,
	rawContent,
	url
}, Symbol.toStringTag, { value: 'Module' }));

export { _page as _ };
